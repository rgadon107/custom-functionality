# Styles Directory

This directory holds the full version of the plugin's stylesheet(s).  The minified (uglified) version(s) is(are) stored in the `dist` folder.

If you intend to modify theme styles, it's better to create a child theme and add those style changes to a child-theme stylesheet.
